import 'package:flutter/material.dart';

class InnerHomePage extends StatelessWidget {
  const InnerHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inner Home Page'),
    );
  }
}
